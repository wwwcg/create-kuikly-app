plugins {
    kotlin("multiplatform").version("{{ohosKotlinVersion}}").apply(false)
    id("com.google.devtools.ksp").version("{{ohosKspVersion}}").apply(false)
{{#if isCompose}}
    id("org.jetbrains.compose").version("{{composeVersion}}").apply(false)
    kotlin("plugin.compose").version("{{ohosKotlinVersion}}").apply(false)
{{/if}}
}
